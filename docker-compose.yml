services:
  app:
    build: .
    ports:
      - "8040:8040"
    restart: always
    container_name: agentops-frontend
    healthcheck:
      test: [ "CMD-SHELL", "wget -q --spider http://0.0.0.0:8040/ || exit 1" ]
      interval: 30s
      timeout: 10s
      retries: 3
