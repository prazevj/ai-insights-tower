services:
  app:
    build: .
    ports:
      - "8040:8040"
    restart: always
    container_name: agentops-frontend
    healthcheck:
      test: [ "CMD-SHELL", "wget --no-verbose --tries=1 --spider http://localhost:8040/ || exit 1" ]
      interval: 30s
      timeout: 10s
      retries: 3
